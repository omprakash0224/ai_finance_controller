# Stable ADK — pinned to tested release
google-adk==2.7.0

# LLM client
google-genai>=1.16.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data & validation
pandas>=2.2.0
pydantic>=2.7.0
faker>=28.0.0

# Razorpay SDK (payment ID schema reference + settlement simulation)
razorpay>=1.4.2

# Utilities
python-dotenv>=1.0.0

# Database — Neon PostgreSQL
psycopg2-binary>=2.9.9

# Caching — Upstash Redis (serverless Redis via HTTP REST API)
# Used by Settlement Q&A three-tier cache (Tier 1: Redis, Tier 2: SQL, Tier 3: LLM)
# Optional: if not installed / credentials not set, cache silently degrades to no-op
upstash-redis>=0.15.0

# Background batch processing — Celery + Redis broker
# celery:   task queue framework for chunked parallel reconciliation
# redis:    redis-py client used by worker tasks to write job status
# flower:   real-time Celery monitoring UI (optional, dev/ops tool)
celery[redis]>=5.4.0
redis>=5.0.0
flower>=2.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0           # required by FastAPI TestClient
pytest-asyncio>=0.23.0
