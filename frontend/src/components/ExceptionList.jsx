// ExceptionList — Phase 3 component
// Shows every exception with agent_reasoning and suggested_action.
export default function ExceptionList() {
  return <div>ExceptionList — coming in Phase 3</div>;
}
